import React from 'react';
import { TimerCx } from '../TimerCx';

export const TimerConfiguration: React.FC<TTimerConfigurationProps> = () => null;

interface TTimerConfigurationProps {
	cx: TimerCx;
}
